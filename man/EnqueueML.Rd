% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RevBayes.R
\name{EnqueueML}
\alias{EnqueueML}
\title{Queue projects for marginal likelihood estimation}
\usage{
EnqueueML(projects, models, overwrite = TRUE)
}
\arguments{
\item{projects}{Character vector of project identifiers.}

\item{models}{Character vector of RevBayes model identifiers.}

\item{overwrite}{Logical. Whether to overwrite existing processed matrices.
Defaults to \code{TRUE}.}
}
\value{
\code{EnqueueML()} invisibly returns \code{NULL}.
The function is called for its side-effect of preparing and queuing
marginal-likelihood analyses on the configured remote cluster.
}
\description{
\code{EnqueueML()} mirrors the workflow of \code{\link[=EnqueueMC]{EnqueueMC()}} but instead enqueues
jobs for marginal likelihood estimation using the stepping-stone or
path-sampling methods in RevBayes.
}
\details{
Each project’s matrix is prepared with \code{\link[=PrepareMatrix]{PrepareMatrix()}} and paired with
model scripts generated via \code{\link[=RevBayes]{RevBayes()}}.
If the marginal likelihood output (\code{\link[=StoneFile]{StoneFile()}}) is absent, SLURM job files
are created using \code{MakeSlurm(ml = TRUE)}.

This function requires that SSH credentials and GitHub authentication have
been configured in \code{.Renviron}, as described in \code{\link[=MakeRepo]{MakeRepo()}}.
}
\seealso{
\code{\link[=EnqueueMC]{EnqueueMC()}}, \code{\link[=RevBayes]{RevBayes()}}, \code{\link[=MakeSlurm]{MakeSlurm()}}, \code{\link[=PrepareMatrix]{PrepareMatrix()}}
}
