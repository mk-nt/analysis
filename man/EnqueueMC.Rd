% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RevBayes.R
\name{EnqueueMC}
\alias{EnqueueMC}
\title{Queue projects for MCMC analysis}
\usage{
EnqueueMC(projects, models, overwrite = TRUE)
}
\arguments{
\item{projects}{Character vector of project identifiers.}

\item{models}{Character vector of RevBayes model identifiers.}

\item{overwrite}{Logical. Whether to overwrite existing processed matrices.
Defaults to \code{TRUE}.}
}
\value{
\code{EnqueueMC()} invisibly returns \code{NULL}.
The function is called for its side-effect of generating and enqueuing
RevBayes MCMC analyses on the configured cluster.
}
\description{
\code{EnqueueMC()} automates the complete setup of MCMC analyses across multiple
projects and model definitions.
It constructs matrices, prepares RevBayes repositories and scripts, and
generates SLURM job files where needed.
}
\details{
For each project in \code{projects}, \code{EnqueueMC()}:
\enumerate{
\item Calls \code{\link[=PrepareMatrix]{PrepareMatrix()}} to ensure up-to-date NEXUS matrices.
\item Uses \code{\link[=RevBayes]{RevBayes()}} to copy or generate RevBayes scripts.
\item Checks for an existing convergence file via \code{\link[=ConvergenceFile]{ConvergenceFile()}}.
\item If missing or incomplete, creates MCMC SLURM job files with
\code{MakeSlurm(ml = FALSE)}.
}

The function uses \code{\link[cli:cli_progress_message]{cli::cli_progress_message()}} to display progress and
finishes with \code{\link[cli:cli_progress_bar]{cli::cli_progress_done()}}.
}
\seealso{
\code{\link[=EnqueueML]{EnqueueML()}} for marginal likelihood estimation,
\code{\link[=RevBayes]{RevBayes()}}, \code{\link[=MakeSlurm]{MakeSlurm()}}, \code{\link[=PrepareMatrix]{PrepareMatrix()}}
}
