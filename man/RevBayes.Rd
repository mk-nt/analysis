% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RevBayes.R
\name{RevBayes}
\alias{RevBayes}
\title{Prepare and submit a new RevBayes analysis job}
\usage{
RevBayes(pID, scriptID, makeSlurm = FALSE)
}
\arguments{
\item{pID}{Character. Project identifier (e.g., \code{"1210"}).}

\item{scriptID}{Character. Identifier of the model script to be used.
Prefix \code{"rm_"} denotes random partitioning.}

\item{makeSlurm}{Logical. Whether to generate SLURM job scripts immediately.
Defaults to \code{FALSE}.}
}
\value{
\code{RevBayes()} invisibly returns \code{TRUE} when script generation and Git
synchronization succeed.
The function is called primarily for its side-effect of populating
and committing RevBayes model repositories.
}
\description{
\code{RevBayes()} constructs all necessary RevBayes input files for a given
project and model specification, synchronizes them with the appropriate
GitHub repository, and (optionally) prepares SLURM submission scripts.
}
\details{
The typical workflow is:
\enumerate{
\item Use \code{\link[=PrepareMatrix]{PrepareMatrix()}} to generate standardized \code{.nex} matrix files.
\item Run \code{\link[=MakeRepo]{MakeRepo()}} to create a GitHub repository for this
project–model combination.
\item Call \code{RevBayes(pID, scriptID)} to populate the repository with
RevBayes model scripts.
\item Use \code{\link[=MakeSlurm]{MakeSlurm()}} or \code{makeSlurm = TRUE} to create SLURM job files.
}

The function draws its templates from the \verb{rbScripts/} directory, performs
placeholder substitution (\verb{\%PID\%}, \verb{\%SCRIPT\%}, \verb{\%MATRIXBASE\%}), and copies
the appropriate matrix subsets (\code{neo.nex}, \code{trans.nex}, or their randomised
variants) into the repository.

When \code{makeSlurm = TRUE}, both MCMC and marginal-likelihood SLURM job files
are generated automatically.
}
\seealso{
\code{\link[=PrepareMatrix]{PrepareMatrix()}}, \code{\link[=MakeRepo]{MakeRepo()}}, \code{\link[=MakeSlurm]{MakeSlurm()}},
\code{\link[=EnqueueMC]{EnqueueMC()}}, \code{\link[=EnqueueML]{EnqueueML()}}
}
