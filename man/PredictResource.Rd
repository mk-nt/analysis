% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Resources.R
\name{PredictResource}
\alias{PredictResource}
\title{Predict resource requirements for a SLURM job}
\usage{
PredictResource(pID, scriptID, ml = TRUE, cores = 16, verbose = FALSE)
}
\arguments{
\item{pID}{Character specifying the project identifier to analyse}

\item{scriptID}{Character specifying the script to run}

\item{ml}{Logical; if true, calculate the marginal likelihood; if false,
run MCMCMC analysis}

\item{cores}{Integer: number of CPU cores to request.}

\item{verbose}{Logical; reserved for future diagnostic output.}
}
\value{
A numeric matrix with three rows (\code{mem}, \code{time}, \code{tmp}) and three
columns (\code{fit}, \code{lwr}, \code{upr}), giving a point estimate and 99\% confidence
bounds for each resource:
\describe{
\item{\code{mem}}{Peak RAM in bytes (comparable to \code{MaxRSS}).}
\item{\code{time}}{Wall-clock time in seconds.}
\item{\code{tmp}}{Temporary disk usage in megabytes.}
}
Values are floored at sensible minima (prior OOM memory, 300 s, etc.).
}
\description{
Fits linear models to the log of completed runs (from \code{\link[=SlurmLog]{SlurmLog()}}) to
predict wall-clock time, peak RAM, and temporary disk usage for a new job.
}
\details{
When previous runs timed out, the function reads the job's stdout file on
the remote server to estimate the fraction of work completed, then
rescales the observed run time to the number of cores requested.
When previous runs were killed for exceeding memory, the largest previously
requested memory is used as a floor.

Predictors used are \code{pID}, \code{model}, \code{inf} (ki/kv), \code{task}, and \code{nCPU},
with graceful fallback to simpler formulae when a predictor has insufficient
coverage in the completed-jobs data.
}
\seealso{
\code{\link[=Cores]{Cores()}} uses these predictions to choose core count and request
time; \code{\link[=SlurmLog]{SlurmLog()}} supplies the historical data.
}
