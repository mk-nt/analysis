% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MakeSlurm.R
\name{MakeSlurmChain}
\alias{MakeSlurmChain}
\title{Submit a chain of checkpoint-resuming SLURM jobs}
\usage{
MakeSlurmChain(
  pID,
  scriptID,
  ml = TRUE,
  hoursPerChunk = 24,
  chunks = NULL,
  margin = 1L,
  replace = FALSE
)
}
\arguments{
\item{pID}{Character specifying the project identifier to analyse}

\item{scriptID}{Character specifying the script to run}

\item{ml}{Logical; if true, calculate the marginal likelihood; if false,
run MCMCMC analysis}

\item{hoursPerChunk}{Numeric: wall-clock hours requested for each chunk.
Defaults to 24 — a compromise between backfill responsiveness and the
overhead of repeated queue waits and checkpoint reloads.}

\item{chunks}{Integer: number of chunks to submit.  If \code{NULL} (default),
chosen automatically from \code{\link[=PredictResource]{PredictResource()}}'s upper-bound projection
plus one chunk of margin, capped at 14.}

\item{margin}{Integer: extra chunks to append beyond the prediction.
Defaults to 1; ignored when \code{chunks} is supplied.}

\item{replace}{Logical: should a corresponding job be cancelled in order to
run this job instead?}
}
\value{
Invisibly returns an integer vector of submitted JobIDs.
On failure, \code{FALSE} with a \code{"reason"} attribute.
}
\description{
Splits a long marginal-likelihood analysis into a series of shorter chunks
that resume from the checkpoint files written by \code{marginal.Rev}
(revbayes#991).  Each chunk depends on the previous via SLURM's
\verb{--dependency=afterany:} clause, so they run strictly in series: chunk K+1
is held \code{PENDING (Dependency)} until chunk K reaches a terminal state
(COMPLETED, TIMEOUT, FAILED, CANCELLED, NODE_FAIL), then enters normal
scheduling.  At most one chunk in the chain is ever \code{RUNNING} at a time.
}
\details{
This is useful when the projected wall-clock time exceeds the cluster's
single-job maximum (\code{.config$maxTime}) but the analysis supports
checkpoint resumption.  Shorter chunks also benefit from SLURM backfill,
typically starting sooner than a single long job.
}
\seealso{
\code{\link[=MakeSlurm]{MakeSlurm()}}; \code{\link[=SCancel]{SCancel()}} to terminate a chain mid-flight
(cancelling chunk K satisfies \code{afterany}, so chunk K+1 will start next).
}
